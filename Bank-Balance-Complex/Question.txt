Create a Class Account with the parameters
acc_no,
name,
balance.

Define a function deposite_money() which takes a parameter
dep_amnt as a number and updates the deposit money to the
balance and returns the updated amount.

Create another function withdraw_money() which takes a
number as with_amnt. There should be a minimum amount
present in the account which is 1000, so if the amount
present in the account after withdrawing money is less
than 1000 then return 0 otherwise return updated amount.

Input:
12345
Rajesh
1200

d
1500
output:
2700

w
2000
output:
insufficient amount
